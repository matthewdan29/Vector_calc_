If a = (a₁, a₂, a₃), then the length of a (also called the norm or magnitiude), denoted ||a||, is √a²₁ + a²₂ + a²₃


		a*a = ||a||²

Now we're ready to state the main result concerning the geometry of the dot product. 
If a and b are two nonzero vectors in R³ (or R²) drawn with their tails at the same point, let Θ, where 0 <= Θ <= π, be the angle between a and b. 
If either a or b is the zero vector, then Θ is indeterminated. 
